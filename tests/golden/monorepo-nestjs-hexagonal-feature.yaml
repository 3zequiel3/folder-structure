root: my-monorepo
tree:
  packages:
    ui:
    types:
    config:
  apps:
    api:
      src:
        modules:
          "{module}":
            domain:
              entities:
              value-objects:
            ports:
              inbound:
              outbound:
            application:
              use-cases:
              dtos:
              mappers:
            infrastructure:
              persistence:
              http:
              events:
      test:
    web:
      src:
        features:
        components:
          ui:
        hooks:
        services:
      public:
